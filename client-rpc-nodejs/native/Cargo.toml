[package]
name = "client-rpc-nodejs"
version = "0.1.0"
authors = ["crypto <crypto>"]
license = "MIT"
build = "build.rs"
exclude = ["artifacts.json", "index.node"]

[lib]
name = "client_rpc_nodejs"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.3.1"

[dependencies]
neon = "0.3.1"
